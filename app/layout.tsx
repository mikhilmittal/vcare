import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Swasthghar | Quality Medical Care at Home",
  description: "Swasthghar delivers quality medical care at the comfort of your home. Book now for elder care, doctor consultations, lab tests, critical care, pharmacy, and ambulance service.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}

